# Windows MSVC: reserve 16 MB of main-thread stack. Required by vulkano's
# runtime SPIR-V parser once the Vulkan viewer lands; harmless until then.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-args=/STACK:16777216"]
