//! Debug game entry: runs the game (same loop as the `game` release binary)
//! with developer tools and displays on top — FPS overlay, log console,
//! state inspector (see `game_debug` lib).
//!
//! `game` stays the clean release entry; this binary is the developer entry.
//! Empty loop for now — mirrors `crates/game/src/main.rs`. Screen rendering
//! and wiring land in M1+.

fn main() {}
