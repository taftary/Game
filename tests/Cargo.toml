[package]
name = "game_tests"
version.workspace = true
edition.workspace = true
publish = false
autotests = false


[lib]
name = "game_tests"
path = "src/lib.rs"

[dependencies]
game_engine = { workspace = true, features = ["test-internals"] }

